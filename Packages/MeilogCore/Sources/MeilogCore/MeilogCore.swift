import Foundation

/// MeilogCore の名前空間。モデルは Model/、MVI は Feature/、protocol は Port/、QR は Payload/ に置く。
public enum MeilogCore {
    /// 交換フォーマットのバージョン（docs/card-payload.md）
    public static let payloadVersion = 1
}
